1.0.52

Safety hardening over 1.0.51. Fail-safe if the pause/resume limit is malformed (treated as "pause now / don't resume" instead of erroring out and skipping the cap). And if a locked charging switch later stops working, ACC now auto-recovers and warns instead of silently overcharging. Bundles ACC v2025.5.18-dev-fix8.
