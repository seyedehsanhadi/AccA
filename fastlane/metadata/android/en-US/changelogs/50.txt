1.0.46

Polish for Pixel / Tensor: removes the brief on/off charge bursts near the limit. ACC now uses the fixed-threshold charge_stop_level switch instead of the live-% "battery/capacity" form that made the firmware re-resume at the threshold, so the limit holds cleanly. Bundles ACC v2025.5.18-dev-fix5.
