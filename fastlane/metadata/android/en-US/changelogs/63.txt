1.1.2

Fixes the battery draining to ~70% instead of holding at your limit. The discharge variant that caused it is gone — charging now just stops at your limit and holds there. Existing installs migrate automatically (no command). Bundles ACC v2025.5.18-stable.2.
