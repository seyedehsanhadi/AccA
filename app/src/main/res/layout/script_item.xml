<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.MaterialComponents.CardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/card_margin"
    android:layout_marginTop="@dimen/card_margin"
    android:layout_marginRight="@dimen/card_margin"
    android:layout_marginBottom="@dimen/card_margindiv2"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="3dp">

    <LinearLayout
        android:id="@+id/item_profile_info"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:visibility="visible">

        <!-- Details Linear Layout -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/card_content_padding">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <!-- Headline5 wrapped most script names onto two lines and gave one card most
                         of the screen. Headline6 keeps the title clearly dominant while fitting
                         on one line, so a list of scripts can actually be scanned. -->
                    <TextView
                        android:id="@+id/item_script_title_tv"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6"
                        tools:text="@tools:sample/full_names" />

                    <TextView
                        android:id="@+id/item_script_description_tv"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:textSize="13sp"
                        tools:text="@tools:sample/full_names" />

                </LinearLayout>

                <ImageView
                    android:id="@+id/edit_config_button"
                    style="@style/ActionIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="?selectableItemBackground"
                    android:visibility="gone"
                    app:srcCompat="@drawable/ic_outline_settings_24px" />

                <!-- Explicit run target. Running a script executes root shell commands against a
                     live charging controller, so it must be aimed at rather than triggered by a
                     stray tap anywhere on the row. -->
                <ImageButton
                    android:id="@+id/item_script_run_ib"
                    style="@style/ActionIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="?selectableItemBackgroundBorderless"
                    android:contentDescription="@string/script_run"
                    app:srcCompat="@drawable/ic_baseline_play_arrow_24" />

                <ImageButton
                    android:id="@+id/item_script_expand_ib"
                    style="@style/ActionIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="?selectableItemBackgroundBorderless"
                    android:contentDescription="@string/script_expand"
                    app:srcCompat="@drawable/ic_baseline_arrow_drop_down_24px" />

                <ImageButton
                    android:id="@+id/item_script_options_ib"
                    style="@style/ActionIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:srcCompat="@drawable/ic_outline_more_vert_24px" />

            </LinearLayout>

            <!-- The body is a shell command: monospace makes flags and paths readable, and 13sp
                 keeps a two-line collapsed preview compact. -->
            <TextView
                android:id="@+id/item_script_body_tv"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="monospace"
                android:paddingTop="@dimen/card_margindiv2"
                android:textSize="13sp"
                tools:text="@tools:sample/full_names" />

    </LinearLayout>
</LinearLayout>
</com.google.android.material.card.MaterialCardView>